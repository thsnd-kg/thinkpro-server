import { MongoConfig } from './mongo.config';
import { JwtConfig } from './jwt.config';
import { AppConfig } from './app.config';
import { BcryptConfig } from './bcrypt.config';

export default [MongoConfig, JwtConfig, AppConfig, BcryptConfig];
